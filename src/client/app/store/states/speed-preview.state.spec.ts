// import { SomeClass } from './speed-preview.state';

export function main() {
  xdescribe('Some Class', () => {
    // let stateUnderTest: SomeClass;

    beforeEach(() => {
      // stateUnderTest = new SomeClass(null, null, null);
    });

    xit('has no tests!', () => {
      expect(true).toBe(true);
    });
  });
}
