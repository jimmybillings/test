// import { SomeClass } from './snackbar.state';

export function main() {
  describe('Some Class', () => {
    // let stateUnderTest: SomeClass;

    // beforeEach(() => {
    //   stateUnderTest = new SomeClass(null, null, null);
    // });

    it('has no tests!', () => {
      expect(true).toBe(true);
    });
  });
}
