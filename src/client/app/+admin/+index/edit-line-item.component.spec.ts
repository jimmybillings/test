import { EditLineItemComponent } from './edit-line-item.component';

export function main() {
  describe('Edit Line Item Component', () => {
    let componentUnderTest: EditLineItemComponent;

    beforeEach(() => {
      componentUnderTest = new EditLineItemComponent();
    });

    it('has no tests!', () => {
      expect(true).toBe(true);
    });
  });
}
