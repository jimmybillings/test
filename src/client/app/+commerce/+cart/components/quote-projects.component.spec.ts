import { QuoteProjectsComponent } from './quote-projects.component';

export function main() {
  describe('Quote Projects Component', () => {
    let componentUnderTest: QuoteProjectsComponent;

    beforeEach(() => {
      componentUnderTest = new QuoteProjectsComponent();
    });

    it('has no tests!', () => {
      expect(true).toBe(true);
    });
  });
}
