import { WzInputSuggestionsComponent } from './wz-input-suggestions.component';

export function main() {
  describe('Wz Input Suggestions Component', () => {
    let componentUnderTest: WzInputSuggestionsComponent;

    beforeEach(() => {
      componentUnderTest = new WzInputSuggestionsComponent(null, null, null, null);
    });

    it('has no tests!', () => {
      expect(true).toBe(true);
    });
  });
};

