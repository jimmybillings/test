div(*ngIf="config || components")
  app-header([currentUser]="currentUser", [config]="components.header")
  .main-content
    router-outlet
  app-footer([currentUser]="currentUser", [config]="components.footer")
  