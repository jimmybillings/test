div(*ngIf="header")
  app-header([currentUser]="currentUser", [config]="(header | async)", (onLogOut)="logout()")
  .main-content
    router-outlet
  app-footer([currentUser]="currentUser", [config]="(footer | async)")
